<?xml version="1.0" encoding="UTF-8"?>
<FindBugsFilter
        xmlns="https://github.com/spotbugs/filter/3.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://github.com/spotbugs/filter/3.0.0 https://spotbugs.readthedocs.io/en/latest/filter.html">

    <!-- Exclude Lombok-generated code -->
    <Match>
        <Class name="~.*\$_.*" />
    </Match>

    <!-- Exclude test classes -->
    <Match>
        <Class name="~.*Test.*" />
    </Match>

    <!-- Exclude Lombok Builder classes -->
    <Match>
        <Class name="~.*\$.*Builder.*" />
    </Match>

    <!-- Exclude JPA entities from exposure checks (normal for entities) -->
    <Match>
        <Class name="~.*\.domain\.*" />
        <Bug pattern="EI_EXPOSE_REP,EI_EXPOSE_REP2" />
    </Match>

    <!-- Exclude DTOs from exposure checks (normal for DTOs) -->
    <Match>
        <Class name="~.*\.api\.*" />
        <Bug pattern="EI_EXPOSE_REP,EI_EXPOSE_REP2" />
    </Match>

    <!-- Exclude infrastructure classes from exposure checks -->
    <Match>
        <Class name="~.*\.infrastructure\.*" />
        <Bug pattern="EI_EXPOSE_REP,EI_EXPOSE_REP2" />
    </Match>

    <!-- Exclude configuration classes from exposure checks -->
    <Match>
        <Class name="~.*\.config\.*" />
        <Bug pattern="EI_EXPOSE_REP,EI_EXPOSE_REP2" />
    </Match>

    <!-- Exclude Serializable classes from serialVersionUID requirement (not required for our use case) -->
    <Match>
        <Bug pattern="SE_BAD_FIELD,SE_NO_SERIALVERSIONID" />
    </Match>

    <!-- Exclude Spring Boot main class from certain checks -->
    <Match>
        <Class name="com.renzo.labs.ticketing.EventTicketingSystemApplication" />
        <Bug pattern="EI_EXPOSE_REP,EI_EXPOSE_REP2" />
    </Match>

    <!-- Exclude @PrePersist methods (JPA lifecycle callbacks) -->
    <Match>
        <Method name="onCreate" />
        <Bug pattern="UPM_UNCALLED_PRIVATE_METHOD" />
    </Match>

</FindBugsFilter>
